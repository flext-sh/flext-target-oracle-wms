# Environment overrides for flext-target-oracle-wms (prefix FLEXT_TARGET_ORACLE_WMS_).
# Copy to .env and fill real values; .env is gitignored — never commit secrets.
FLEXT_TARGET_ORACLE_WMS_ENABLED=true
